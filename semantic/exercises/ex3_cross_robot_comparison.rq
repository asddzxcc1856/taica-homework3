# Exercise 3 · Cross Robot Capability Comparison  (STUDENT TODO)
#
# 目的: 利用共享 ontology 比較不同 Robot 對同一 target 的能力。
# 共用 target URI (hw3:target_*) 是 join key; ?armLength 直接從各機器人
# 的 DH 參數彙總 (SUM |dh_a|, 上臂+前臂), 用來解釋 workspace 差異:
#   UR5 ~0.82 m -> target_mid OUT_OF_REACH
#   UR10 ~1.18 m -> target_mid SOLVED
PREFIX hw3: <http://taica.course/hw3/ontology#>

SELECT *   # STUDENT TODO: 改成題目要求的變數
WHERE {
  # ------------------------- your query -------------------------
  # hint: ?ik hw3:solvesForTarget ?target ; hw3:computedForRobot ?robot ; hw3:hasIKStatus ?status .
  # hint: 臂長估計: 子查詢 { SELECT ?robot (SUM(ABS(?a)) AS ?armLength) WHERE { ?robot hw3:hasJoint/hw3:dh_a ?a } GROUP BY ?robot }
  # ---------------------------------------------------------------
}
