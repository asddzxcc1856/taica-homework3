# Exercise 2b · Monotonicity Check  (reference solution, bonus)
#
# 沿 dul:directlyPrecedes 檢查相鄰樣本殘差是否單調遞減:
# 回傳「殘差不減反增」的相鄰樣本對 — 對收斂良好的 DLS 解預期 0 筆;
# OUT_OF_REACH 軌跡可能出現震盪列, 正是值得討論的現象。
PREFIX hw3: <http://taica.course/hw3/ontology#>
PREFIX dul: <http://www.ontologydesignpatterns.org/ont/dul/DUL.owl#>

SELECT *   # STUDENT TODO: 改成題目要求的變數
WHERE {
  # ------------------------- your query -------------------------
  # hint: ?s dul:directlyPrecedes ?sNext .
  # hint: 比較 ?s / ?sNext 的 hw3:hasSampleResidual, FILTER(?rNext > ?r)
  # ---------------------------------------------------------------
}
