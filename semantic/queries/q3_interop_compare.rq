# Q3 (學生實作): 跨機器人互操作性比較。
#
# 目標: 對每一個共用 target (hw3:target_near / target_mid / target_far),
#        列出「每支機器人」的 IK 求解狀態,產生一張 3 targets × 2 robots
#        共 6 列的比較表,欄位為 ?target ?robot ?status。
#
# 這條查詢展示語意互操作性的核心價值:
#   你的 UR5 pipeline 與助教的 UR10 pipeline 從未見過彼此的程式碼,
#   但因為都用 hw3: 語彙對同一組 target URI 發佈結果,
#   一條 SPARQL 就能回答「哪個目標只有 UR10 搆得到?」
#
# 提示:
#   1. pattern 的主詞是 hw3:IKComputation 的實例
#   2. 你需要的三個 property: hw3:solvesForTarget, hw3:computedForRobot,
#      hw3:hasIKStatus
#   3. 用 ORDER BY ?target ?robot 讓輸出穩定 (評分腳本會逐列比對)
#
# ------------------------- TODO: 完成這條查詢 -------------------------
PREFIX hw3: <http://taica.course/hw3/ontology#>

SELECT ?target ?robot ?status
WHERE {
  # TODO: 在此填入 triple patterns
  # ?ik ...
}
ORDER BY ?target ?robot
